

class mAP(object):
    def __init__(self, config):
        super().__init__()
        
        
    def __call__(self, pred, gt):
        # mAP Calculation
        # pred = [batch, num_anchors, 4 + 1 + num_classes]
        # gt = 
        
        pass